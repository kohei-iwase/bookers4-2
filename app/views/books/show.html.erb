
<div class="row">
	<div class="col-xs-3">
		<h2>User info</h2>
		<%= render 'users/profile', user: current_user %>

		<h2>New book</h2>
		<%= render 'books/newform', book: Book.new %>
	</div>
	<div class="col-xs-9">
		<h2>Book detail</h2>

		<%= render 'books/detail', book: @book %>

	<!--コメント部分-->
	  <%= render 'comment_area', book: @book,book_comment: @book_comment %>
	</div>
</div>

